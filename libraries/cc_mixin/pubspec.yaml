name: cc_mixin
description: Reusable mixins for the project.
version: 1.0.0
publish_to: none

environment:
  sdk: '^3.11.5'

resolution: workspace

dependencies:
  cc_sdk:
  cc_sdk_ui:
  flutter:
    sdk: flutter
  get: ^4.7.3
  loadmore: ^2.1.0
  multiple_result: ^5.3.0
  custom_refresh_indicator: ^4.0.1
  lottie: ^3.3.3
  theme:

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
