name: widget
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  app_config:
    path: ../app_config

  cc_sdk:
    path: ../../libraries/cc_sdk
  cc_sdk_ui:
    path: ../../libraries/cc_sdk_ui

  flutter:
    sdk: flutter
  lottie:
  modal_bottom_sheet:
  path_provider:
  provider:

  reusable:
    path: ../reusable

  shared_preferences:
  theme:
    path: ../theme

  url_launcher:
  permission_handler:

dev_dependencies:
  flutter_lints:
  flutter_test:
    sdk: flutter